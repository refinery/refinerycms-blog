<ul id='sortable_list'>
  <%= render :partial => '/refinery/blog/admin/posts/post', :collection => @posts %>
</ul>
<%= render :partial => "/refinery/admin/sortable_list",
           :locals => {
             :continue_reordering => (defined?(continue_reordering) ? continue_reordering : true)
           } %>
